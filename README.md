# Brew Haven Café

A premium multi-page café website built with React + Vite + TypeScript + Tailwind + Framer Motion.

## Run
```
npm install
npm run dev
```

## Build
```
npm run build
```

## Deploy
Push to GitHub, then import into Vercel. Framework: **Vite**. Build: `npm run build`. Output: `dist`.
